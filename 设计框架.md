# Omni-Harness 2026: 通用 Agent 开发壳工程设计方案

## 1. 核心设计理念

Omni-Harness 旨在构建一个跨职能、零门槛、插拔式的数字员工底座。它不再预设用户是产品、设计或运营，而是将一切任务转化为"可编排的能力流"。

- **架构解耦**：将底座（Orchestration）、工具（Tools）、记忆（Memory）与监控（Observability）彻底分离。
- **复杂度分层**：根据任务的"思考深度"而非"职业角色"来组织代码。
- **人机协作**：原生内置"审批闸口"，让非技术用户在 UI 上掌控 AI 的关键决策。

## 2. 基于任务复杂度的三阶编排 (Orchestration Tiers)

通过 AI 需要执行的逻辑回路深度，将编排分为三个标准等级：

| 级别 | 类型 | 逻辑特征 | 典型应用场景 |
|------|------|----------|-------------|
| L1 | 原子反应器 (Atomic) | 单跳 ReAct 模式，即问即答 | 文件格式转换、单次网页查询、文案翻译 |
| L2 | 标准工作流 (Sequence) | 线性 DAG，有明确的 SOP 步骤与条件分支 | 自动周报、需求文档撰写、竞品对比表格 |
| L3 | 自主执行器 (Autonomous) | 带有"自我反思"与"逻辑修正"的闭环循环 | 深度调研（循环验证）、自动化代码优化、脑暴 |

## 3. 插件化能力体系 (Pluggable Capabilities)

### A. 基础工具包 (Built-in Tools)

壳工程出厂即带的通用能力：

- **文件操作**：`read_file`, `write_file`, `inspect_dir`, `search_in_files`
- **网页感知**：`web_browse` (基于 Playwright)、`quick_search`
- **多模态提取**：`visual_parse` (OCR/图表转文本)、`schema_refiner` (非结构化转 JSON)
- **协议支持**：原生支持 MCP (Model Context Protocol)，可动态挂载外部服务器

### B. 三层记忆总线 (Memory Bus)

- **短期记忆 (Thread-based)**：基于 SQLite，记录当前会话的上下文，支持状态回溯（Time Travel）
- **长期偏好 (User Profile)**：记录跨会话的用户习惯（如：特定的文案风格、特定的技术栈偏好）
- **语义知识 (Semantic RAG)**：动态挂载向量数据库插件，为 Agent 提供企业内部手册或知识库

### C. 质量与安全网关 (Guardrails)

- **结构化校验**：使用 Pydantic 强制模型输出符合定义的 Schema，失败时触发自动修复节点
- **预算控制**：内置 `Max_Tokens` 和 `Max_Loops` 拦截器，防止自主 Agent 陷入逻辑死循环
- **隐私脱敏**：双向过滤敏感词与个人身份信息（PII）

## 4. LangSmith 监控与评估体系

LangSmith 不仅是调试工具，更是壳工程的"生产后视镜"：

- **全链路追踪 (Tracing)**：为非技术用户提供"思考链"可视化，透明化展示 AI 的每一个决策步长
- **自动化评测 (Evaluation)**：通过建立"黄金数据集"，量化 Prompt 修改或编排逻辑变动后的准确率
- **成本审计**：按部门、按任务复杂度监控 Token 消耗与 ROI（投入产出比）

## 5. 项目结构建议 (插件化导向)

```
omni-harness/
├── core/                        # 核心底座（不可变逻辑）
│   ├── factory.py               # 图工厂：根据配置生成 L1/L2/L3 实例
│   ├── state.py                 # 全局统一状态定义 (BaseState)
│   └── safety.py                # 预算控制与内容安全拦截
├── plugins/                     # 插拔式功能区
│   ├── tools/                   # 文件、网页、MCP 工具适配器
│   ├── memory/                  # 不同维度的存储策略插件
│   └── observers/               # LangSmith 与自定义日志插件
├── graphs/                      # 编排模板
│   ├── l1_reactor.py            # 简单反应器模板
│   ├── l2_workflow.py           # 线性 SOP 流程模板
│   └── l3_executor.py           # 自主迭代执行器模板
├── langgraph.json               # 壳工程入口配置
└── user_config.yaml             # 用户自定义：加载哪些工具，开启哪级编排
```

## 6. 核心开发建议

- **实现"工具自介绍"**：让插件在加载时自动通过 Python 文档字符串（Docstrings）向 Agent 介绍自己的功能
- **强制"审批流"**：在 L2 和 L3 模板的所有"写操作"节点前，默认开启 `interrupt_before`
- **标准化输出**：确保每一个 Agent 的输出都通过一个 `StandardOutput` 节点，将其转化为 Markdown 或 JSON 供后续工具直接使用
